const window_height = window.innerHeight
const window_width = window.innerWidth


export const patent_style = {
    height: (window_height - 40).toString()+'px' 
}

export const wrapper_style = {
    gridGap: Math.round(window_height/20).toString()+'px',
    fontSize: Math.round(window_height/28).toString()+'px'
}

export const keno_box_style = {
    fontSize: Math.round(window_height/25).toString()+'px'
}

export const child_anim_style = {
    fontSize: Math.round(window_height/40).toString()+'px'
}

export const reSizefontSize = {
    fontSize: Math.round(window_height/40).toString()+'px'
}

export const drawsNum_style = {
    fontSize: Math.round(window_height/9).toString()+'px'
}