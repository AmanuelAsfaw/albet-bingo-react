import React, { FC, Fragment } from 'react';

import { NotFoundPropType } from './NotFound.utils';

const NotFound: FC<NotFoundPropType> = ({ children }) => {
    return (
        <Fragment>
            
        </Fragment>
    );
};

export default NotFound;